interface:
  display_name: "Repository Remediation"
  short_description: "Verify and fix repository findings"
  default_prompt: "Use $code-remidate to verify supplied findings and implement the smallest safe, validated fixes."
